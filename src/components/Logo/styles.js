import styled from 'styled-components';

export const Container = styled.div`
  width: 100%;
  display: flex;
  gap: 1.7rem;
  align-items: center;
  font-family: var(--font-secondary);

  > p {
    font-weight: 700;
    font-size: 3.7rem;
  }
`;
